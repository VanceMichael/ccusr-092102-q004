# 教师课程改革证据服务

本项目用于保存不同国家和院校的教师教育课程版本、能力框架与改革试点材料。跨体系比较必须保留原始定义、映射意见和适用范围，不能只依赖相同或相近的课程名称。

运行服务：

```bash
go run ./cmd/server
```

运行检查：

```bash
go test ./...
```

`fixtures/curriculum.json` 展示课程方案与能力框架的最小关联方式。
